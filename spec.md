# Puppy Spec

`spec.md` 只记录当前生效的产品事实与边界。未实际验证的外部页面能力必须显式标注，不能用静态测试替代。

## 产品定位

- `Puppy` 是项目 owner 自用的本地 Web 工具，只服务 1 到 2 个小红书账号。
- 产品通过独立可见浏览器完成公开网页浏览和交互，使用 AI 理解当前可见的笔记与评论上下文并生成评论或回复。
- 产品目标是提供可观察、可暂停、可恢复、可逐条批准和可人工接管的关键词互动任务。

## 当前阶段与事实

- 当前阶段：`runtime_validation`。本地 Web 工作台、主干代码、依赖、任务状态、测试和文档已经建立，完整真实登录链路仍需在当前页面重新验证。
- 根目录 `app.py` 是唯一终端入口；生产代码位于 `puppy/` 包，测试位于 `tests/`。
- `XhsPost（人工）.py` 是 owner 明确要求保留且禁止修改的人工版本，不属于机器人入口。
- 旧自动脚本、旧评论数据和源码 Cookie 使用方式不构成当前事实或兼容合同。
- 当前开发和目标运行环境是 Windows、Python 3.12、Playwright 1.58。
- AI Provider 配置只从根目录 `.env` 读取；当前默认使用硅基流动 `https://api.siliconflow.cn/v1`、Chat Completions 和 `Pro/zai-org/GLM-5.1`。进程环境和任务参数不是 AI 配置源。
- `.env.example` 保存同结构占位配置和最近一次真实模型目录注释；真实 `.env` 被 Git 忽略，API Key 不进入源码、任务、证据或健康报告。
- 任务只有 `approval` 批准模式和 `auto` 自动模式。批准模式默认逐条等待批准；自动模式只读取任务参数并连续执行，不进入草稿批准状态。
- `python app.py` 在 `127.0.0.1` 随机端口启动 Web 工作台，使用每次启动随机生成的本机访问 Token，并自动打开系统浏览器。
- Web 工作台可以编辑 `.env` 的已知 AI 配置字段，但 API Key 不回显；空输入保留原 Key，只有显式清除操作可以删除。
- 工作台保留非终态任务供恢复，但只有独立浏览器会话实际运行时才显示登录、验证码或安全校验的人工接管弹窗；启动控制台本身不构成实时页面检测。
- 工作台运行记录按服务端事件序号直接投影执行器写入脱敏证据的真实事件，包括搜索结果加载、笔记打开、上下文读取、评论或回复点击发送、详情关闭和停止原因；刷新或事件流重连只补投缺失序号，前端不得自行追加业务步骤，也不得用定时器或预设步骤模拟进度。
- 浏览器资料代表一个独立账号环境。现有 `.puppy/profile/Default` 由“默认浏览器”继续使用；新建浏览器使用独立 Chromium Profile，拥有干净且彼此隔离的登录态。
- 同一时刻只允许一个任务执行线程；每个浏览器资料最多有一个未结束任务。控制台可创建、暂停、继续、停止任务，并处理草稿批准和未决写入。
- 专用 Chromium 在工作台运行期间保持独立可见；同一时刻只启动一个浏览器资料。手动停止浏览器时先暂停该资料绑定的未结束任务，再完整关闭专用 Chromium；正常关闭 `python app.py` 时先暂停正在执行的任务，再关闭浏览器。浏览器资料、任务和证据继续保留。
- 浏览器资料、任务状态、AI 草稿和脱敏证据保存在 Git 忽略的 `.puppy/`，保留到 owner 手工删除。
- 当前浏览器会话和资料索引位于 `.puppy/state/`，当前任务位于 `.puppy/state/tasks/`；当前主干不读取旧位置中的会话或任务状态。

## 用户路径

1. 用户运行 `python app.py`，系统启动本机 Web 工作台并自动打开系统浏览器。
2. 用户在控制台填写或保存 AI 配置，刷新模型目录并按需执行健康检查或真实生成测试。
3. 用户选择“继续使用”复用已有浏览器登录态，或选择“新建浏览器”创建干净资料，然后在独立可见 Chromium 中人工完成登录。
4. 用户设置关键词、笔记上限、回复范围、批准或自动模式、间隔和每日限额，然后创建任务。
5. 系统通过可见搜索控件提交关键词，跨虚拟列表滚动轮次累计唯一笔记 ID。
6. 系统逐篇打开笔记，重新读取当前可见正文和评论上下文，再由 AI 生成笔记评论。
7. 批准模式由用户批准并发送、编辑后批准、跳过或暂停草稿；自动模式直接进入已检查的页面写入步骤。
8. 输入内容校对通过后点击当前可见发送控件即记为已派发；随后随机选择配置范围内的已有评论逐条生成并发送回复。
9. 每篇完成后关闭详情并确认列表恢复，再进入下一篇；达到任务笔记上限后完成。
10. 登录失效、验证码或安全校验触发阻断弹窗并保留现场；用户处理后点击继续，系统重新检查页面门禁，通过后才恢复任务。

## 状态与恢复

- 任务状态包括 `created`、`running`、`waiting_login`、`waiting_approval`、`waiting_human`、`paused`、`complete`、`failed` 和 `stopped`。
- `complete`、`failed` 和 `stopped` 是终态，不从终态恢复。
- 任务创建时永久绑定当前浏览器资料。暂停和恢复不会改变资料；当前浏览器资料不一致时不得继续、批准或裁决该任务。
- 每次写入先持久化 `in_flight` 意图，发送控件点击返回后立即落盘为已派发。任务记录已评论笔记 ID、已回复评论指纹、当前笔记、待批准草稿和写入时间。
- 恢复执行不得重复提交已经记账成功的笔记评论或评论回复。
- 进程在提交与结果落盘之间退出时，`resume` 不得猜测结果；用户检查页面后必须用 `resolve --result sent|not-sent` 裁决。
- 每个任务有笔记上限、回复数范围和每日写入硬上限；连续 30 轮滚动仍持续增量加载时暂停并报告 `limit_reached`，不得声称滚到底。
- `pause` 和 `stop` 在动作边界生效。暂停任务可继续，停止任务不可继续；停止浏览器只暂停绑定该浏览器资料的未结束任务。
- 浏览器进程意外退出时，执行中的任务保存为可恢复暂停；重新启动任务绑定的浏览器资料后可以继续。

## 页面交互边界

- 浏览、搜索、打开笔记、评论和回复只通过 Playwright 驱动的真实可见浏览器完成。
- 登录、扫码、验证码和安全校验由用户人工完成；系统不识别、不点击、不绕过验证。
- 系统不逆向私有接口、不破解签名、不伪造浏览器指纹，也不隐藏自动化驱动。
- 页面元素每轮从当前 DOM 动态发现，并验证可见、可编辑和当前点击命中层。临时 ID、完整 class、固定坐标和一次详情路径不是长期合同。
- 搜索结果按窗口文档滚动处理；虚拟列表当前节点数不等于累计加载总数。
- 搜索路由变化不代表结果已经加载；只有实际识别到相对提交前新增的搜索结果笔记链接后才能进入详情与写入流程。等待 30 秒仍无法证明新结果已加载时暂停并保留现场，不得沿用旧列表或按零结果正常完成。
- 写入前核对输入区真实值；点击当前可见发送控件后直接进入下一动作，不轮询输入清空、新增文本、计数或 Toast。点击动作抛错时停止，不自动重试。
- 关闭详情优先使用当前可见关闭控件，其次只点击经 hit-test 证明属于遮罩本身的空白点；操作后必须验证详情消失和列表恢复。

## AI 与发送边界

- AI 只接收当前可见笔记正文、有限评论上下文和明确的目标评论，不接收 Cookie、localStorage、Authorization 或签名参数。
- 提示要求输出单段可直接发送的中文正文，不虚构亲历，不包含个人敏感信息、营销引流或重复套话。
- 笔记评论最多 120 个字符，回复最多 80 个字符；空内容或超限内容不得进入页面输入区。
- 批准模式要求每条草稿明确批准；自动模式代表 owner 对当前任务参数范围内生成内容的连续发送授权，不扩大账号或平台权限。
- AI 请求失败不触发页面写入；缺失或非法 `.env` 必须明确失败，Provider 临时失败使任务进入可恢复的暂停状态。
- `models` 返回当前 API Key 可见的动态模型 ID；`health` 使用只读模型目录检查，只有显式 `health --generate` 才额外创建一次真实最小生成。

## 数据与隐私

- Cookie、登录态和本地存储只存在专用浏览器资料目录，不写入源码、普通日志、任务 JSON 或证据。
- 证据 URL 移除查询参数，并对 token、authorization 和 web session 字段脱敏。
- 页面证据只保存当前视口截图、脱敏 HTML、有限文本摘要、结构化检查结果和事件记录。
- 当前主干不读取、不迁移、不依赖旧评论数据。

## 已验证页面事实

以下事实来自 2026-08-02 的旧实时探针，只是当前适配器的优先候选，使用前仍必须重新验证：

- 关键词 `AI Agent` 的搜索结果由窗口文档滚动和虚拟列表持续增量加载；累计 139 个唯一笔记时仍没有可证明的有限底部。
- 搜索区当时有两个重叠 `textarea`，必须结合可见性与 `elementFromPoint` 选择当前命中层。
- 图片笔记详情、评论滚动区、输入区、提交、回复和点赞控件曾被找到。
- 当前账号自己的图片笔记曾完成取消并恢复点赞 `17 -> 16 -> 17`，并完成一条回复，页面评论数 `11 -> 12`。
- 点击经 hit-test 确认的详情遮罩空白区曾成功关闭详情并恢复列表。

## 当前未完成的外部验证

- 新包结构下的完整“搜索 -> 评论笔记 -> 随机回复 -> 关闭详情 -> 下一篇”真实登录链路。
- 视频笔记详情和整篇笔记评论提交。
- 验证码、登录失效和频率提示的暂停后恢复。
- 收藏与关注；它们不在当前任务执行器中。
- 针对真实小红书上下文的生成质量评估；连接健康检查不能替代业务内容评估。

## 已验证 Provider 事实

- 2026-08-02 使用本地 `.env` 中的 owner API Key 成功访问硅基流动模型目录，返回 91 个模型 ID，默认模型 `Pro/zai-org/GLM-5.1` 在目录中。
- 同日通过 OpenAI Python SDK 的 Chat Completions 接口完成真实最小生成，返回“连接正常”；健康报告未输出 API Key。

## 当前不做

- 不建设公开注册、多租户 SaaS、计费、云端账号托管或远程管理台。
- 不提供私有接口逆向、签名破解、混淆绕过、验证码自动处理或自动化隐藏。
- 不保留旧脚本入口、旧 schema、旧数据或旧状态兼容层。
- 不自动关注、收藏或点赞；历史单动作验证不构成当前任务能力。

## 验收标准

- 完整静态验证命令通过：
  - `python -W error -m compileall -q app.py puppy tests`
  - `python -m pytest`
  - `python app.py --help`
- 批准模式和自动模式的边界在 Web 工作台与 CLI 中清楚可见。
- `.env` 是 AI Provider、API Key、Base URL、模型和 wire API 的唯一配置源；命令与 Web 工作台使用同一配置与检查边界。
- Web API 只接受本次启动的本机访问 Token，修改请求校验同源 Origin，任何配置响应都不得包含 API Key。
- 验证码、登录失效或安全校验弹窗的继续操作必须重新检查当前页面门禁，不能把人工点击本身当作验证通过。
- 任务恢复不重复已经记账成功的笔记评论或回复。
- 新建浏览器获得独立的干净登录态；继续使用浏览器复用所选资料；停止浏览器不删除登录态、任务或证据。
- 暂停任务、停止任务和停止浏览器是三个不同操作，状态与恢复能力不得混用。
- 登录态、Cookie 和密钥不进入源码、日志、任务或版本控制。
- 真实页面验收必须保存当次脱敏证据，并分别报告图片、视频、评论、回复和人工接管的已验证与未验证结果。

## 授权边界

- 默认协议为 MIT，以根目录 `LICENSE` 为准。
- 默认贡献分支为 `master`；commit、push、发布和部署仍需 owner 明确授权。
