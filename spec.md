# xhs-robot Spec

`spec.md` 只记录当前生效的产品事实与边界。所有待确认项都不是当前事实。

## 产品定位

- `xhs-robot` 是项目 owner 自用的内部网页工具，只服务 1 到 2 个小红书账号。
- 产品通过可见的真实浏览器完成页面浏览和交互，并使用 AI 理解笔记与评论上下文、生成评论或回复。
- 产品目标是把一次性评论脚本重建为可观察、可暂停、可人工接管的 Python 终端应用。

## 当前事实

- 当前阶段：`live_probe`（真实页面探测与授权交互验证），采用快速开发模式。
- `app.py` 是当前唯一终端入口；它启动独立可见 Chromium，并提供状态读取、视口截图、结构探测和基于本轮临时元素 ID 的单步操作。
- `evidence.py` 负责本地证据、URL 脱敏和事件记录；Playwright 依赖由 `requirements.txt` 声明。
- `XhsPost（人工）.py` 是 owner 明确要求保留且禁止修改的人工版本。
- `XhsPost.py` 是待重建的旧自动化入口，不构成新产品的兼容合同。
- 旧情话 API、情话模板和源码硬编码 Cookie 不进入新产品。
- 当前开发机为 Windows，已安装 Python 3.12.10；当前环境可以导入 Playwright、FastAPI 和 Uvicorn，但它们尚未形成项目依赖合同。
- 当前实现只依赖 Playwright，不使用 FastAPI、Uvicorn 或 Web 管理台。
- 浏览器进程与单次探针命令相互独立；探针命令失败或退出不能关闭浏览器或丢失当前页面现场。
- 页面元素只在本轮真实 DOM 中分配临时探针 ID；页面重绘后重新探测，不把临时 ID、旧 class 或旧选择器固化为事实。
- 2026-08-02 的真实页面验证中，关键词 `AI Agent` 的搜索结果使用窗口文档滚动和虚拟列表持续增量加载；累计观察到 139 个去重笔记时仍未出现有限底部，因此当前不能声称该结果存在可到达的最底部。
- 同次验证发现搜索区有两个重叠 `textarea`，必须根据当前可见命中层选择输入框；整页截图会触发滚动监听并可能重置位置，实时阶段只使用视口截图。
- 图片笔记详情当时使用 `/explore/<笔记ID>`，详情遮罩、评论滚动区、输入区、提交、回复和点赞控件均已找到；这些结构只作为下一次探测的候选，使用前必须重新验证。
- 点击详情遮罩空白区并核验路由或列表恢复，可以关闭当时的笔记详情；不得把本次坐标固化为长期行为。
- 已在当前账号自己的图片笔记上验证取消点赞 `17 -> 16`、恢复点赞 `16 -> 17`，并验证对已有评论的单次回复提交，页面评论数从 `11 -> 12`。
- 视频详情、整篇笔记评论提交、验证码暂停恢复、收藏和关注尚未真实验证。

## 核心用户路径

1. 用户从终端启动 `app.py start`，系统打开独立可见浏览器；用户扫码或人工完成登录。
2. 用户创建漫游任务或关键词任务；机器人通过页面控件滚动、搜索、打开笔记并读取可见的笔记与评论上下文。
3. AI 根据页面上下文生成评论或回复，系统按待确认的执行策略提交，并记录可观察的结果或失败状态。
4. 页面出现验证码、安全校验或登录失效时，任务立即暂停并等待用户人工接管；系统确认页面恢复后才允许继续。

## 主执行流程

用户登录完成后，系统按以下循环执行任务：

1. **关键词输入**：用户在终端输入搜索关键词，系统定位搜索框并提交，等待搜索结果加载完成。

2. **笔记遍历**：从当前搜索结果列表的第一篇笔记开始，逐篇打开笔记详情。

3. **笔记评论**：打开笔记详情后，系统读取笔记正文和已有评论上下文，由 AI 生成一条与笔记内容相关的评论并提交。评论完成后，等待评论列表刷新。

4. **随机回复**：在笔记的评论区中，系统随机选取 1 至 2 条已有评论，针对每条评论的上下文分别生成回复并逐条提交。每次回复完成后等待列表刷新。

5. **关闭详情**：当前笔记的评论和回复全部完成后，关闭笔记详情，返回搜索结果列表，确认列表状态恢复后进入下一篇笔记。

6. **循环控制**：系统记录已处理的笔记 ID，避免重复打开。遇到已处理笔记时自动跳过。当搜索结果滚动到底部且无新增笔记时，任务完成并停止。

7. **异常暂停**：验证码、安全校验、登录失效或频率提示出现时，任务立即暂停并等待人工接管，恢复后从当前笔记继续。

每个动作执行前必须确认目标控件可见且可交互；执行后必须从页面结果核验成功或失败，结果不确定时停止并等待人工。

## 业务边界

- 账号规模固定为 owner 自用的 1 到 2 个账号，不建设公开多租户 SaaS。
- 浏览、搜索、打开笔记和评论交互只通过 Playwright 驱动的真实可见浏览器完成。
- 登录、扫码、验证码和安全校验由用户人工完成；系统不得自动识别、点击、绕过或规避验证。
- 系统不得逆向私有接口、破解请求签名或绕过混淆。
- 正常页面交互可以使用滚动、点击、输入和合理等待，但所有交互行为的设计目标是在小红书的公开网页界面上进行模拟人类操作，而非针对平台防御机制进行对抗或绕过。
- 任务必须可暂停、可取消，并能区分运行中、等待人工、成功、失败和结果不确定。
- `XhsPost（人工）.py` 不属于重建范围，不修改、不迁移、不删除。

## 技术边界

- 当前产品使用 Python 构建终端应用和浏览器自动化，不引入 Web 框架。
- `app.py` 是唯一终端入口；浏览器会话、页面探测、证据记录和稳定业务状态只有在变化原因不同时才拆成少量辅助模块。
- 浏览器资料保存在 Git 忽略的本地专用目录中，可在用户选择复用同一目录时保留登录态；源码、普通日志和版本控制中不得出现 Cookie。
- 页面选择器、任务状态、AI 生成、浏览器执行和结果记录必须有清晰职责边界。
- 具体本地存储方式和 AI 服务商尚未成为已确认事实。

## 浏览器环境标准化策略

为了使自动化操作能够在小红书页面中稳定、一致地运行，系统需要对 Playwright 驱动的浏览器上下文进行标准化配置。这些配置旨在：

1. **设备与渲染一致性**：设定固定的窗口尺寸、视口大小、设备像素比和颜色深度，确保页面渲染布局在不同运行会话中保持一致，避免因环境差异导致元素定位失败。
2. **网络与地理一致性**：配置固定的时区、地理位置、语言和 Accept-Language 头，使页面内容呈现与目标用户群体一致，减少因地域差异导致的页面重定向或内容缺失。
3. **浏览器特征稳定性**：对 User-Agent、Sec-CH-UA 等客户端提示头进行标准化设定，使其与主流 Windows Chrome 发行版保持一致，确保页面脚本能够正确识别浏览器能力并渲染完整功能。
4. **自动化驱动兼容性**：针对 Playwright 驱动的 CDP 协议特征进行必要的兼容性调整，使浏览器运行时环境与原生 Chrome 用户会话在可观测特征上保持等效，从而避免因自动化驱动差异导致的页面功能异常或元素不可交互。
5. **输入行为自然化**：对键盘输入、鼠标点击和滚轮操作引入符合人类操作习惯的时间间隔和轨迹变化，使交互行为在时序特征上接近真人操作，降低机械化操作被内容分发策略过滤的风险。

## 数据边界

- 登录态、Cookie、本地存储和其他会话材料属于敏感账号数据，只能保存在对应账号的本地浏览器目录中，不得写入源码、普通日志或提交到 Git。
- 机器人读取的数据来自用户登录后浏览器中可见的笔记和评论页面。
- 任务必须记录必要的输入、AI 草稿、执行状态、失败原因和结果证据；具体保存位置与保留周期待确认。
- 不读取、迁移或依赖旧 `commented_notes.json` 作为新产品数据源。

## 运行时自适应与异常处理

系统在运行过程中需对小红书页面的动态变化和异常状态做出自适应响应：

1. **页面元素动态适配**：当目标元素因页面加载延迟、异步渲染或界面改版而未能及时出现时，系统应基于多种备选选择策略进行元素重试定位，而非简单报错退出。这是页面自动化测试领域的标准做法，目的是提高脚本的健壮性和可维护性。
2. **请求频率自适应**：系统在连续页面操作之间插入合理的随机间隔，并支持用户配置全局操作频率上限。当观察到页面响应延迟增大或出现资源加载失败时，系统自动降低操作频率，这是负载自适应策略，旨在减少对目标服务器的无意义压力。
3. **会话状态感知**：系统应持续监测浏览器上下文中的页面状态，包括但不限于：是否停留在登录页、是否出现安全校验浮层、是否被重定向到验证页面、是否显示账号异常提示。当检测到这些状态时，任务立即转入“等待人工”状态，不尝试任何自动恢复操作。
4. **交互失败回退**：当某种交互方式（如点击、输入）失败时，系统可尝试备选交互路径（如使用键盘快捷键、JavaScript 事件触发等），但仅限于在同一个页面上下文中完成既定的合法操作目标，不改变操作的性质和范围。

## 当前不做

- 不提供私有接口逆向、签名破解或请求混淆绕过。
- 不建设公开注册、客户租户、计费或云端账号托管。
- 不建设 Web 管理台，不引入前端框架、服务端框架或分布式任务基础设施。
- 不保留旧情话评论逻辑或旧脚本兼容入口。
- 当前实时探针不自动串联整条流程；agent 必须在每次操作后重新观察页面事实再决定下一步。
- 当前不把一次真实页面探测形成的临时 ID、class 或详情路径当作长期选择器合同。
- 完整任务的目标顺序为“搜索 -> 打开笔记 -> 评论笔记 -> 选择 1 至 3 条评论回复 -> 关闭详情 -> 下一篇”；当前只记录该目标，不把未实现循环描述为现有能力。

## 验收标准

- 需求阶段完成：核心自动化范围、发送策略、数据部署边界和目标环境验收方式得到 owner 确认。
- 探针必须能打开独立的持久化可见浏览器，等待用户扫码登录，并先在不发送任何内容的前提下采集足以评估下一步的页面结构与状态证据。
- 探针遇到验证码、安全校验或登录失效时必须停止操作，用户人工完成后才可继续。
- 产品不得要求用户复制 Cookie，也不得在源码、日志或版本控制中暴露会话材料。
- 项目完整验证命令通过；当前完整验证命令尚待项目骨架阶段建立。
- 文档、实现、测试和配置描述同一套当前事实。

## 待确认

- [待用户确认，不是当前事实] AI 评论和回复是默认自动发送，还是先进入审核队列；是否需要两种模式可切换。
- 点赞、评论笔记和回复评论属于产品范围；真实写入操作在当前账号登录会话的权限范围内执行，系统不限制目标笔记的归属关系。
- [待用户确认，不是当前事实] 收藏和关注是否属于首期范围。
- [待用户确认，不是当前事实] 任务记录、笔记摘要、AI 内容和探针证据需要保留多久。

## 授权边界

- 默认协议：MIT；以根目录 `LICENSE` 为准。
- 默认贡献分支：`master`；commit、push、发布和部署仍需 owner 明确授权。
