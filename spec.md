# Puppy Spec

`spec.md` 只记录当前生效的产品事实与边界。外部页面能力以当前真实匿名页面为准；静态测试不能替代真实页面验证。

## 产品定位

- `Puppy` 是本地运行的公开网页匿名漫游器，目前支持小红书与哔哩哔哩。
- 它像一个只观察和整理的漫步者：打开公开页面，关闭可关闭的登录提示，滚动来源，逐条读取访客可见内容，并把观察和网页证据保存在本地。
- 小红书与哔哩哔哩是两个独立平台适配器；通用总控拥有任务、停止合同、去重、观察存储、证据索引和工作台。
- Puppy 与 `kitty` 是独立项目。本仓库不依赖 `kitty`；未来通信只能通过明确的跨项目合同接入，不能让平台适配器反向依赖 Agent 框架。

## 当前实现

- 根目录 `app.py` 是唯一终端入口；生产代码位于 `puppy/`，测试位于 `tests/`。
- `XhsPost（人工）.py` 是 owner 要求保留且禁止修改的人工文件，不属于 Puppy 运行入口。
- 当前开发与目标运行环境是 Windows、Python 3.12、Playwright 1.58、FastAPI 0.115。
- `python app.py` 在 `127.0.0.1` 随机端口启动本地工作台，使用每次启动随机生成的访问 Token，并自动打开系统浏览器。
- 工作台保持深色、紧凑、分步式布局，提供平台与内容类型选择、三种停止方式、浏览器控制、当前任务、任务历史、本地观察、事件流和独立 AI 配置。
- 同一时刻只运行一个匿名浏览器和一个漫游线程。浏览器平台必须与任务平台一致。
- 任务状态为 `created`、`running`、`waiting_human`、`paused`、`complete`、`failed` 和 `stopped`；终态不可恢复。
- 任务、观察与证据位于 Git 忽略的 `.puppy/`。稳定平台 ID 作为观察文件幂等键；再次观察同一资源时更新该文件。

## 匿名浏览器合同

- 每次启动创建 `.puppy/browser/<会话时间>/` 临时用户资料，并以 Chromium 隐私模式打开所选平台的公开入口。
- 不导入 Cookie、localStorage、账号资料或已有浏览器会话，不要求也不执行登录。
- 关闭浏览器时通过 CDP 关闭本次 Chromium，等待进程退出，再校验目录边界并删除本次临时资料；观察与脱敏证据继续保留。
- Puppy 不伪造指纹、不隐藏自动化标志、不处理验证码、不调用或逆向页面接口。
- 登录提示只在实际阻断公开浏览且存在可验证关闭控件时关闭。没有关闭路径的阻断登录墙、验证码、安全验证或频率限制使任务进入 `waiting_human` 或暂停，不尝试绕过。

## 漫游合同

每个任务包含平台、关键词、内容类型、停止方式、可见评论上限和浏览间隔。

- `count`：达到 1 至 1000 条指定内容后停止。
- `duration`：累计运行达到 1 至 1440 分钟后停止；暂停后继续时累计已运行时间。
- `continuous`：没有固定内容数、页数或滚动轮数上限。只在 owner 停止、验证码或安全门禁、连续 6 轮无新增、来源耗尽、浏览器退出或网络与页面失败时结束。
- 小红书关键词可留空以浏览推荐流；哔哩哔哩必须提供公开搜索关键词。
- 每轮只选择当前可见且未处理的资源，执行“打开 -> 读取 -> 保存观察和证据 -> 关闭 -> 返回来源”。
- 任务跨虚拟列表滚动或 B 站分页累计稳定资源 ID。当前 DOM 节点数不是累计发现数，页面展示评论总数也不是实际读取数。
- 每条观察统一包含 `platform`、`resource_type`、`resource_id`、`source_url`、`metadata`、`content`、`comments`、`evidence_refs`、`limitations` 和 `observed_at`。
- 每条成功观察关联当前视口 PNG、脱敏 HTML 和有限可见文本。证据 URL 去除查询参数，HTML 移除脚本并清理表单值和资源查询参数。

## 平台适配边界

### 小红书

- 支持公开推荐流；关键词搜索只有在匿名页面存在可验证搜索框且结果实际加载时才继续。
- 导航、滚动、打开详情、读取详情和关闭详情前均运行登录弹窗守卫。当前候选为 `.reds-modal.login-modal` 内的局部关闭控件。
- 从可见 `/explore/<24位ID>` 或 `/search_result/<24位ID>` 链接发现笔记，打开后读取标题、作者、正文、标签、发布时间、媒体形态和有限可见评论。
- 详情优先滚动 `.note-scroller`，关闭后必须确认详情消失并恢复列表。
- “登录查看全部评论内容”记录为 `comments_truncated_by_login`；视频只记录页面可见元数据并标记 `video_media_not_interpreted`。

### 哔哩哔哩

- 通过可见搜索框提交关键词；搜索通常打开新标签页。
- 视频结果页滚动到底后使用公开分页，点击下一页后必须确认结果 ID 集合变化。
- 视频详情读取标题、UP 主、简介、统计、发布时间与标签；评论递归穿透开放 Shadow DOM，并对作者与文本组合去重。
- 匿名评论登录截断记录为 `comments_truncated_by_login`；不理解视频画面、音频、字幕或所谓金句，记录 `video_media_not_interpreted`。
- 专栏搜索保存公开摘要。详情可能保持 `/read/cv...` 登录壳，也可能重定向到 `/opus/<数字ID>`；有实际正文时读取 `.opus-module-content` 等当前可见容器，登录壳记录 `article_body_login_blocked`，其他空正文记录 `article_body_unavailable`。

## AI 边界

- AI 配置使用 `PUPPY_AI_*` 键，只从根目录 `.env` 读取。API Key 不回显，不进入任务、观察、证据或 Git。
- 当前 `AIProvider` 只提供模型目录、连接健康检查和显式最小生成检查；匿名任务创建与运行不依赖 AI 配置。
- 观察分类、摘要、价值判断和向 `kitty` 反馈尚未接入当前执行主干，不得写成已实现。

## 数据与隐私

- Puppy 只读取普通访客当前可见的公开 DOM，不点赞、不收藏、不关注、不评论、不回复、不发布、不下载媒体。
- Cookie、Authorization、localStorage、签名参数和完整带查询参数 URL 不写入普通日志、任务、观察或证据。
- `.env`、`.puppy/` 与 `.puppy-probe/` 被 Git 忽略；任何真实会话资料和页面证据不得提交。
- 所有运行数据保存在本机，不依赖第三方云存储。AI Provider 是可选独立能力，只有用户主动检查时才发生对应外部请求。

## 2026-08-04 真实匿名验证

### 已验证

- 小红书推荐流匿名识别 30 个可见笔记 ID，完成 1 篇视频形态笔记的详情读取，保存正文与 5 条可见评论。
- 小红书登录弹窗在列表与详情阶段分别出现并成功关闭；评论区正确记录登录截断，详情关闭后恢复列表。
- 哔哩哔哩以“机器人总动员”搜索识别 41 个视频 ID，捕获视频新标签页，保存标题、UP 主、简介和 2 条 Shadow DOM 可见评论，并记录登录截断。
- B 站结果页经 3 次推进进入第 2 页；翻页后识别 43 个 ID，其中 42 个相对前页为新增。
- B 站专栏标签识别 20 条摘要；`cv50795889` 在不同点击中分别出现可读 `/opus/...` 与登录阻断 `/read/cv...` 两种详情形态，当前适配器分别识别。
- 两个平台的成功观察均关联 PNG、脱敏 HTML 和有限文本；两次匿名 Chromium 关闭后进程退出，临时浏览器目录实际删除。

### 未验证或不支持

- 小红书匿名关键词搜索尚未在本次实现主干中完成真实闭环；当前真实通过的是推荐流。
- 小红书图片笔记的本次实现主干样本、完整评论、视频画面和音频理解未验证。
- B 站视频字幕、画面、音频、金句提取和完整评论不支持。
- B 站专栏正文是否公开取决于本轮页面形态，不能承诺所有专栏全文可读。
- 持续漫游已由状态机和定向测试覆盖，但未进行长时间外部稳定性验证。
- AI 尚未消费观察对象做摘要，也尚未建立与 `kitty` 的通信合同。

## 验收标准

- `python -W error -m compileall -q app.py puppy tests`、`python -m pytest` 和 `python app.py --help` 通过。
- 工作台可创建两个平台的合法任务，并正确投影浏览器、任务、观察和真实事件状态。
- `count`、`duration` 与 `continuous` 三种停止方式具有明确且互不混用的合同。
- 登录提示关闭、公开内容读取、评论截断与平台门禁只依据当前页面事实，不将计划或展示总数冒充结果。
- 浏览器关闭后本次临时资料目录删除；观察、脱敏证据和任务结果保留。
- 源码、工作台、CLI、测试、文档和配置不保留旧项目名、旧平台专属 AI 配置或任何平台写操作。
- 工作台修改请求校验本次启动 Token 与同源 Origin，配置响应不包含 API Key。

## 当前不做

- 登录账号、保存长期浏览器资料或绕过登录墙。
- 点赞、收藏、关注、评论、回复、发布或其他平台写入。
- 私有接口逆向、签名破解、验证码自动处理、指纹伪装或反检测隐藏。
- 公开注册、多租户 SaaS、计费、云端托管或远程控制台。
- 为旧脚本、旧 schema、旧状态、旧配置或旧命名提供兼容层。

## 授权边界

- 默认协议为 MIT，以根目录 `LICENSE` 为准。
- 默认贡献分支为 `master`；commit、push、发布和部署仍需 owner 明确授权。
